Place theme layer images here for `world_tutorial`.
Current placeholders used by `world.config.json`:
- `layer-stars.svg`
- `layer-city.svg`
- `layer-ground.svg`

You can replace these with `.png` or `.svg` files and update paths in:
- `js/level/world_tutorial/world.config.json`
